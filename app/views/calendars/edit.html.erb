<% provide(:title, "Edit #{@calendar.name.length>0? @calendar.name : @category.name}") %>
<div class="row">
  <div class="col s12">
    <h1>Edit <%=@calendar.name.length>0? @calendar.name : @category.name%></h1>
  </div>
</div>
<div class="row">
  <div class="col s12 m9">
    <%= render 'form' %>
  </div>
</div>
