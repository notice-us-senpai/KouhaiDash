<%= form_for(@task_assignment) do |f| %>
  <% if @task_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_assignment.errors.count, "error") %> prohibited this task_assignment from being saved:</h2>

      <ul>
      <% @task_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.number_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :membership_id %><br>
    <%= f.number_field :membership_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
