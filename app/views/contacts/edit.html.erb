<% provide(:title, 'Update #{@contact.name}') %>
<div class="row">
	<h1>Update <%= @contact.name %></h1>
	<p>Please conduct data validation testing on the user and login system instead.</p>
</div>
<div class="row">
	<%= render 'form' %>
</div>
