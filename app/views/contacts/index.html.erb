<% provide(:title, 'Contacts') %>
<div class="row">
	<h1>Contacts</h1>
</div>

<div class="row">
	<%= link_to "Create New Contact", { action: "new" } %>
	<!--
	| <%= link_to "Download as CSV", group_category_contacts_path(@group, @category, format: "csv") %>
	-->
</div>


<div class="row">
<%@contacts.each do%>

	<div class="col m6">
		<div class="contact-text">
			<ul class="contact-details">
				<li><%= contact.name %></li>

				<li><%= contact.organisation %></li>

				<li><%= contact.email %></li>
				<li><%= contact.phone %></li>
			</ul>

			<div class="text-center">
				<%= link_to "Read More", { action: "show", id: contact } %> |
				<%= link_to "Update", { action: "edit", id: contact } %> |
				<%= link_to "Delete", { action: "destroy", id: contact },
					method: :delete, data: { confirm: "Delete #{contact.name}?" } %>
			</div>
		</div>
	</div>
<%end%>
</div>
<div class="row">
	<%= link_to "Create New Contact", { action: "new" } %>
	<!--
	| <%= link_to "Download as CSV", group_category_contacts_path(@group, @category, format: "csv")  %>
	-->
</div>
