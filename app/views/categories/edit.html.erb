<% provide(:title, "Update #{@category.name}") %>
<div class="row">
  <div class="col s12">
	   <h1>Update <%= @category.name %></h1>
   </div>
</div>

<div class="row">
  <div class="col s12 m9">
    <%= render 'form' %>
  </div>
</div>
